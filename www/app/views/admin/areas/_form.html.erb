<%= form_for(@admin_area) do |f| %>
  <% if @admin_area.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_area.errors.count, "error") %> prohibited this admin_area from being saved:</h2>

      <ul>
      <% @admin_area.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label '城市' %><br>
    <%= f.text_field :city, :class => "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label 'Google標記_X' %><br>
    <%= f.number_field :google_map_x, :class => "form-control", :step => 0.000001 %>
  </div>
  <div class="form-group">
    <%= f.label 'Google標記_Y' %><br>
    <%= f.number_field :google_map_y, :class => "form-control", :step => 0.000001 %>
  </div>
  <div class="actions">
    <%= f.submit '更新 地區', :class => "btn btn-primary btn-full"  %>
  </div>
<% end %>
