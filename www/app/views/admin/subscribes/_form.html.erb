<%= form_for(@admin_subscribe) do |f| %>
  <% if @admin_subscribe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_subscribe.errors.count, "error") %> prohibited this admin_subscribe from being saved:</h2>

      <ul>
      <% @admin_subscribe.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
